fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27
sentence-transformers>=3.0
pillow>=10.0
rapidfuzz>=3.0
scikit-learn>=1.4
numpy>=1.26

# Optional extras (both are large downloads):
# rembg       background removal before photo matching (run the API with REMOVE_BG=1)
# easyocr     reads names and numbers on found items automatically
